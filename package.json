{
  "name": "web-client-server-app-ts",
  "author": "Wink Saville",
  "version": "0.1.0",
  "private": false,
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/winksaville/web-client-server-app-ts"
  },
  "scripts": {
    "build:common": "tsc -p src/common/tsconfig.json",
    "build:common.spec": "yarn build:common & tsc -p src/common/tsconfig.spec.json",
    "pretest:common": "yarn build:common.spec",
    "test:common": "alsatian 'build/common/**/*.spec.js' --tap",
    "pretest:travis": "yarn build:common",
    "test:travis": "yarn test:common",
    "pretest": "yarn build:common",
    "test": "yarn test:common"
  },
  "devDependencies": {
    "@types/mithril": "github:spacejack/mithril.d.ts",
    "@types/selenium-webdriver": "^2.53.40",
    "alsatian": "^1.2.1",
    "typescript": "^2.2.1",
    "webpack": "^2.2.1",
    "selenium-webdriver": "^3.1.0"
  },
  "dependencies": {
    "debug": "^2.6.1",
    "mithril": "^1.0.1"
  }
}
