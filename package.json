{
  "name": "web-client-server-app-ts",
  "author": "Wink Saville",
  "version": "0.1.0",
  "private": false,
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/winksaville/web-client-server-app-ts"
  },
  "scripts": {
    "_build:common": "tsc -p src/common/tsconfig.json",
    "_build:common.spec": "tsc -p src/common/tsconfig.spec.json",
    "_build:server": "tsc -p src/server/tsconfig.json",
    "_build:server.spec": "tsc -p src/server/tsconfig.spec.json",
    "_test:common": "alsatian 'build/common/**/*.spec.js' --tap",
    "_test:server": "alsatian 'build/server/**/*.spec.js' --tap",
    "pretest": "yarn _build:common && yarn _build:server && yarn _build:common.spec && yarn _build:server.spec",
    "test": "yarn _test:common && yarn _test:server",
    "test:travis": "yarn test"
  },
  "devDependencies": {
    "@types/mithril": "github:spacejack/mithril.d.ts",
    "@types/selenium-webdriver": "^2.53.40",
    "alsatian": "^1.2.1",
    "typescript": "^2.2.1",
    "webpack": "^2.2.1",
    "selenium-webdriver": "^3.1.0"
  },
  "dependencies": {
    "debug": "^2.6.1",
    "mithril": "^1.0.1"
  }
}
